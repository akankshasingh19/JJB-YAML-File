- job:
    name: parameter
    parameters:
      - string:
          name: param1
          default: bar
          description: "Hi!!This is a parameter module"
      - bool:
          name: param2
          default: false
          description: "A parameter named FOO, defaults to 'false'."
      - choice:
          name: project
          choices:
            - nova
            - glance
          description: "On which project to run?"
